<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Player</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheet/index.css">
</head>

<body>
    <div class="container mt-5">
        <h1 class="text-center">Edit Player</h1>
        <form action="/player/<%= player._id %>/edit?_method=PUT" method="POST" enctype="multipart/form-data">
            <div class="mb-3">
                <label for="name" class="form-label">Name</label>
                <input type="text" class="form-control" id="name" name="name" value="<%= player.name %>" required>
            </div>
            <div class="mb-3">
                <label for="age" class="form-label">Age</label>
                <input type="number" class="form-control" id="age" name="age" value="<%= player.age %>" required>
            </div>
            <div class="mb-3">
                <label for="gender" class="form-label">Gender</label>
                <select class="form-select" id="gender" name="gender" required>
                    <option value="Male" <%= player.gender === 'Male' ? 'selected' : '' %>>Male</option>
                    <option value="Female" <%= player.gender === 'Female' ? 'selected' : '' %>>Female</option>
                    <option value="Other" <%= player.gender === 'Other' ? 'selected' : '' %>>Other</option>
                </select>
            </div>
            <div class="mb-3">
                <label for="sport" class="form-label">Sport</label>
                <select class="form-select" id="sport" name="sport" required>
                    <option value="Cricket" <%= player.sport === 'Cricket' ? 'selected' : '' %>>Cricket</option>
                    <option value="Chess" <%= player.sport === 'Chess' ? 'selected' : '' %>>Chess</option>
                    <option value="Archery" <%= player.sport === 'Archery' ? 'selected' : '' %>>Archery</option>
                </select>
            </div>
            <div class="mb-3">
                <label for="profileImage" class="form-label">Profile Image</label>
                <input type="file" class="form-control" id="profileImage" name="profileImage">
                <div class="mt-2">
                    <img src="/uploads/<%= player.profileImage %>" alt="Profile Image" class="img-thumbnail" width="150" height="150">
                </div>
            </div>

            <% if (player.sport === 'Archery') { %>
            <div class="mb-3">
                <h3>Archery Details</h3>
                <div class="mb-3">
                    <label for="totalArrowsShot" class="form-label">Total Arrows Shot</label>
                    <input type="number" class="form-control" id="totalArrowsShot" name="totalArrowsShot" value="<%= details.totalArrowsShot %>" required>
                </div>
                <div class="mb-3">
                    <label for="bullsEyes" class="form-label">Bulls Eyes</label>
                    <input type="number" class="form-control" id="bullsEyes" name="bullsEyes" value="<%= details.bullsEyes %>" required>
                </div>
                <div class="mb-3">
                    <label for="accuracy" class="form-label">Accuracy</label>
                    <input type="number" step="0.01" class="form-control" id="accuracy" name="accuracy" value="<%= details.accuracy %>" required>
                </div>
            </div>
            <% } %>

            <% if (player.sport === 'Cricket') { %>
            <div class="mb-3">
                <h3>Cricket Details</h3>
                <div class="mb-3">
                    <label for="runs" class="form-label">Runs</label>
                    <input type="number" class="form-control" id="runs" name="runs" value="<%= details.runs %>" required>
                </div>
                <div class="mb-3">
                    <label for="wickets" class="form-label">Wickets</label>
                    <input type="number" class="form-control" id="wickets" name="wickets" value="<%= details.wickets %>" required>
                </div>
                <div class="mb-3">
                    <label for="strikeRate" class="form-label">Strike Rate</label>
                    <input type="number" step="0.01" class="form-control" id="strikeRate" name="strikeRate" value="<%= details.strikeRate %>" required>
                </div>
            </div>
            <% } %>

            <% if (player.sport === 'Chess') { %>
            <div class="mb-3">
                <h3>Chess Details</h3>
                <div class="mb-3">
                    <label for="gamesPlayed" class="form-label">Games Played</label>
                    <input type="number" class="form-control" id="gamesPlayed" name="gamesPlayed" value="<%= details.gamesPlayed %>" required>
                </div>
                <div class="mb-3">
                    <label for="gamesWon" class="form-label">Games Won</label>
                    <input type="number" class="form-control" id="gamesWon" name="gamesWon" value="<%= details.gamesWon %>" required>
                </div>
                <div class="mb-3">
                    <label for="ranking" class="form-label">Rating</label>
                    <input type="number" class="form-control" id="rating" name="rating" value="<%= details.rating %>" required>
                </div>
            </div>
            <% } %>

            <button type="submit" class="btn btn-primary">Save Changes</button>
            <a href="/player/<%= player._id %>" class="btn btn-secondary">Cancel</a>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
