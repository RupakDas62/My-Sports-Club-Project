<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Player Details</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheet/onePlayer.css">
</head>

<body>
    <div class="container">
        <div class="row justify-content-center mt-5">
            <div class="col-md-5">
                <div class="card">
                    <div class="card-header">
                        Player Details
                    </div>
                    <div class="card-body d-flex justify-content-center align-items-center">
                        <div class="player-details">
                            <h2 class="player-name">
                                <%= player.name %>
                            </h2>
                            <div class="player-info" id="player-age">
                                <span class="detail-label">Age:</span>
                                <span class="detail-value">
                                    <%= player.age %>
                                </span>
                            </div>
                            <div class="player-info" id="player-gender">
                                <span class="detail-label">Gender:</span>
                                <span class="detail-value">
                                    <%= player.gender %>
                                </span>
                            </div>
                            <% if (player.sport==="Cricket" ) { %>
                                <div class="player-info" id="run-scored">
                                    <span class="detail-label">Run Scored:</span>
                                    <span class="detail-value">
                                        <%= details.runs %>
                                    </span>
                                </div>
                                <div class="player-info" id="wickets-taken">
                                    <span class="detail-label">Wickets Taken:</span>
                                    <span class="detail-value">
                                        <%= details.wickets %>
                                    </span>
                                </div>
                                <div class="player-info" id="strike-rate">
                                    <span class="detail-label">Strike Rate:</span>
                                    <span class="detail-value">
                                        <%= details.strikeRate %>
                                    </span>
                                </div>
                                <% } else if (player.sport==="Chess" ) { %>
                                    <div class="player-info" id="games-played">
                                        <span class="detail-label">Games Played:</span>
                                        <span class="detail-value">
                                            <%= details.gamesPlayed %>
                                        </span>
                                    </div>
                                    <div class="player-info" id="games-won">
                                        <span class="detail-label">Games Won:</span>
                                        <span class="detail-value">
                                            <%= details.gamesWon %>
                                        </span>
                                    </div>
                                    <div class="player-info" id="ranking">
                                        <span class="detail-label">Rating:</span>
                                        <span class="detail-value">
                                            <%= details.rating %>
                                        </span>
                                    </div>
                                    <% } else if (player.sport==="Archery" ) { %>
                                        <div class="player-info" id="total-arrows">
                                            <span class="detail-label">Total Arrows Shot:</span>
                                            <span class="detail-value">
                                                <%= details.totalArrowsShot %>
                                            </span>
                                        </div>
                                        <div class="player-info" id="bulls-eye">
                                            <span class="detail-label">Bull's Eye:</span>
                                            <span class="detail-value">
                                                <%= details.bullsEyes %>
                                            </span>
                                        </div>
                                        <div class="player-info" id="accuracy">
                                            <span class="detail-label">Accuracy:</span>
                                            <span class="detail-value">
                                                <%= details.accuracy %>
                                            </span>
                                        </div>
                                        <% } %>
                                            <!-- Add more player details as needed -->
                        </div>
                        <img src="/uploads/<%= player.profileImage %>" class="img">

                    </div>

                </div>
                <div class="back-btn d-flex justify-content-center">
                    <a href="/" class="btn btn-primary back-button">Back</a>
                </div>
                <% if(isAdmin) {%>
                    <div class="edit-btn d-flex justify-content-center">
                        <a href="/player/<%=player._id%>/edit" class="btn btn-info back-button">Edit</a>
                    </div>
                    <form action="/player/<%=player._id%>/delete?_method=DELETE" method="POST">
                        <div class="edit-btn d-flex justify-content-center">
                            <button class="btn btn-danger">Delete</button>
                        </div>
                    </form>
                    <%}%>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>